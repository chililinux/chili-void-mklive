import { Global, Color, Palette } from 'styled-components';
export declare const generateColors: (palette: Palette) => Color;
export declare const getColor: (global: Global, color: string) => string;
